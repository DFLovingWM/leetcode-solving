/**
 * 滑动窗口：O(N)
 */
var minSubArrayLen = function(lowerbound, nums) {
  let L = -1, R = -1
  let sum = 0
  let minLength = Infinity
  while (L < nums.length) {
    if (sum < lowerbound) {
      
    }
  }
};