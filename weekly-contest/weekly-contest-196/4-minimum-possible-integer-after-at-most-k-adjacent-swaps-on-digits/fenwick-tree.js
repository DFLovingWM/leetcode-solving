/**
 * 贪心
 */
var minInteger = function(num, k) {
  
};

class FenwickTree {
  constructor(nums) {
    const n = nums.length;
    this.arr = Array.from({ length: n + 1 }, () => 0);
    for ()
  }

  _lowBit() {
    
  }
}

module.exports = minInteger;